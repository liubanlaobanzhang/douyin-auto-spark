# 2026-08-20

## ProductEnv 分支：通知区分手动/定时触发
- ProductEnv 分支由用户在 GitHub 远程创建（从 origin/main 最新提交分出），本地用 `git checkout ProductEnv` 跟踪远程（不是从本地 main 新建，第一次误建后已删除重来）。
- 功能：通知通过 `EVENT_NAME: ${{ github.event_name }}` 环境变量识别触发方式；`workflow_dispatch` 时 Python 脚本给标题追加「【手动触发】」、内容末尾追加 `\n\n【手动触发】`（双换行保证 Markdown 分段渲染；定时触发 schedule 无标记）。
- 已提交：`f8ccaef feat: 通知区分手动触发与定时触发`（仅改 renew-fire.yml，14 行新增）。
- 推送失败：本机无 GitHub HTTPS 存储凭据（credential helper helper-selector 无 tty 无法弹窗）、SSH 公钥未注册 GitHub（Permission denied publickey）、GCM 交互式授权在此环境不弹窗。待用户自行 `git push origin ProductEnv` 或提供凭据。
- 本地验证方式：mock urlopen + 环境变量注入 EVENT_NAME 两种取值，断言标题/内容标记（脚本在 %TEMP%\test_trigger.py）。
